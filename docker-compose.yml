services:
  collector:
    build: .
    volumes:
      - .:/app
      - ./output:/output
    command: >
      sh -c "python -m src.main"
  
  mailer:
    build: .
    volumes:
      - .:/app
      - ./output:/output
    command: >
      sh -c "python -m src.mailer"