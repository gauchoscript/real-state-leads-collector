services:
  collector:
    build: .
    volumes:
      - ./src:/app
      - ./output:/output
    command: >
      sh -c "python main.py"
  
  mailer:
    build: .
    volumes:
      - ./src:/app
      - ./output:/output
    command: >
      sh -c "python mailer.py"